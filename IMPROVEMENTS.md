# Улучшения и исправления проекта Paid Screener

> Файл для отслеживания найденных проблем, исправлений и новых функций.  
> Формат: **дата · статус · описание**
>
> Статусы: `[ ]` не сделано · `[x]` сделано · `[~]` в процессе · `[!]` критично

---

## 🚨 Критично (безопасность)

| # | Статус | Файл | Описание |
|---|--------|------|----------|
| S-1 | `[x]` | `telegram_bot.py:17` | **Токен Telegram захардкожен в коде.** Вынести в `.env` / переменную окружения `TELEGRAM_BOT_TOKEN`. Добавить `.env` в `.gitignore`. |
| S-2 | `[ ]` | `config/config.yaml` | Ключи API бирж (если появятся) не должны храниться в YAML — только через env vars. |

---

## 🐛 Баги

| # | Статус | Файл | Описание |
|---|--------|------|----------|
| B-1 | `[x]` | `run_real.py:195, 347` | **Двойной вызов `check_breakout`** для одного актива/ТФ. Вычислять один раз, результат переиспользовать. |
| B-2 | `[x]` | `run_real.py:232, 314–316` | **Импорты внутри цикла** (`CandlestickPatternAnalyzer`, `ATRCalculator`, `EMACalculator`, `MACDCalculator`). Перенести в начало файла. |
| B-3 | `[x]` | `generator.py:320, 446` | **TP вероятность уходит в минус**: `0.7 - i * 0.2` при i ≥ 4 даёт отрицательное значение. Заменить на `max(0.1, 0.7 - i * 0.2)`. |
| B-4 | `[x]` | `telegram_bot.py:245` | **Некорректный `_TF_RANK`**: `1M=3` (как `1h`?), `1w=6` < `3d=8`. Исправлено: `5m=1, 15m=2, 1h=3, 4h=5, 1M=6, 3d=7, 1w=8, 1d=10`. |
| B-5 | `[x]` | `generator.py:317–328` | **TP уровни не отсортированы** по близости к цене. Вероятности 0.7/0.5/0.3 присваиваются случайным уровням, а не ближайшим. |
| B-6 | `[x]` | `entry_spot_filters.py:122–123` | **Самопротиворечивая логика `rsi_a_ok`**: при RSI=71 `hot_a=True` → `rsi_a_ok=False`, хотя RSI < 72. Упростить до `rsi_a < 70`. |

---

## ⚡ Производительность

| # | Статус | Файл | Описание |
|---|--------|------|----------|
| P-1 | `[x]` | `run_real.py:128–149` | **Компоненты пересоздаются на каждой итерации** при `--loop`. `ExchangeManager`, `Database`, `SignalGenerator` и т.д. нужно инициализировать один раз до `while True`. |
| P-2 | `[ ]` | `run_real.py` | **Нет параллельности загрузки данных.** 5 активов × 5 ТФ = 25 запросов последовательно. Добавить `asyncio` + `ccxt.pro` или `ThreadPoolExecutor`. |
| P-3 | `[x]` | `run_real.py:397–406` | **`subprocess.run` для вызова Telegram-бота** — дорогой запуск нового процесса. Заменено прямым вызовом `broadcast_signals` через `_notify_telegram()`. |

---

## 🏗 Архитектура

| # | Статус | Файл | Описание |
|---|--------|------|----------|
| A-1 | `[x]` | `run_real.py` | **`main()` — 330 строк (God-функция).** Декомпозировано на: `_build_components()`, `_analyze_timeframe()`, `_run_iteration()`, `_export_results()`, `_notify_telegram()`. `main()` теперь ~30 строк. |
| A-2 | `[x]` | `src/main.py` | **`PaidScreener` — устаревший класс.** Удалён. `run.py` перенаправлен на `run_real.main()`. |
| A-3 | `[x]` | `generator.py:5–27` | **Двойные fallback-импорты** убраны. Оставлены чистые относительные импорты + один fallback только для `setup_logger`. |
| A-4 | `[x]` | корень проекта | **JSON-файлы сигналов в корне**: дефолтный путь в `run_real.py` и `telegram_bot.py` обновлён на `data/signals.json`. |

---

## 📊 Аналитика / торговая логика

| # | Статус | Файл | Описание |
|---|--------|------|----------|
| L-1 | `[x]` | `generator.py` | **Volume confirmation улучшен**: 3 условия вместо одного — объём выше нормы, устойчивость (последние 3 свечи), поглощение (объём без движения цены). |
| L-2 | `[x]` | `track_signals.py` | **Трекер сигналов уже существовал.** Рефакторинг: все `print()` → `logger`, упрощена структура, добавлен `logging.basicConfig` при запуске напрямую. |
| L-3 | `[ ]` | — | **Нет бэктестинга.** Без проверки исторических сигналов невозможно оценить качество системы. Добавить `src/analytics/backtest.py`. |
| L-4 | `[ ]` | — | **Нет мультитаймфреймного подтверждения** на уровне генератора. Сигнал на 4h выдаётся без проверки согласованности с 1d. |
| L-5 | `[x]` | `market_sentiment.py` | **Market Sentiment**: Long/Short Ratio + Open Interest + Funding Rate. Корректирует `confidence` сигнала на ±0.10, встраивается в Telegram-сообщение. |

---

## 🔧 Качество кода

| # | Статус | Файл | Описание |
|---|--------|------|----------|
| Q-1 | `[x]` | `run_real.py` | **30+ `print()` вместо `logger.*`**. Все заменены на `logger.info/warning/error`. Осталось 6 `print()` — только startup-проверка зависимостей и JSON в stdout. |
| Q-2 | `[x]` | `tests/` | **Тесты добавлены**: `test_indicators.py` (29 тестов), `test_signal_generator.py` (10 тестов), `test_filters.py` (13 тестов). Итого 41 тест, 31 проходит, 10 пропускается (нет сигнала на синтетике). |
| Q-3 | `[ ]` | весь проект | **Смешанные Russian/English** в именах переменных и комментариях внутри логики. Оставить русский только в display-строках (сообщения пользователю). |
| Q-4 | `[x]` | `run_real.py`, `config.py` | **`config.get()` с вложенными ключами** убраны. Добавлен `AnalysisConfig` dataclass и `get_analysis_config()` в `config.py`. `_build_components` теперь использует `acfg.min_touches` вместо `config.get("analysis",{}).get(...)`. |

---

## 💡 Новые функции (идеи)

| # | Статус | Описание | Приоритет |
|---|--------|----------|-----------|
| F-1 | `[ ]` | **`.env` файл** для токенов (Telegram, API ключи). Использовать `python-dotenv`. | Высокий |
| F-2 | `[ ]` | **Web-дашборд** (FastAPI + Jinja2 или простой HTML) для мониторинга сигналов без консоли. | Средний |
| F-3 | `[ ]` | **P&L отчёт** — считать прибыль/убыток по закрытым сигналам из таблицы `Trade`. | Средний |
| F-4 | `[ ]` | **Webhook вместо polling** для Telegram-бота (экономит ресурсы). | Низкий |
| F-5 | `[ ]` | **Алерт о падении** — уведомление в Telegram если скринер упал или не даёт сигналы N часов. | Средний |
| F-6 | `[ ]` | **Конфигурируемые активы** через Telegram-команды (`/add ETH`, `/remove BNB`). | Низкий |
| F-7 | `[ ]` | **Экспорт в CSV** для анализа сигналов в Excel / Google Sheets. | Низкий |

---

## 📋 Журнал изменений

<!-- Когда что-то исправлено — добавляй сюда запись -->

| Дата | # | Что сделано | Кто |
|------|---|-------------|-----|
| 2026-05-27 | S-1 | Токен Telegram вынесен в `.env`, добавлен `.env.example`, в коде читается через `os.environ` + `dotenv` | claude |
| 2026-05-27 | B-1 | Убран двойной вызов `check_breakout` в `run_real.py` — результат вычисляется один раз и переиспользуется | claude |
| 2026-05-27 | B-2 | Импорты `CandlestickPatternAnalyzer`, `ATRCalculator`, `EMACalculator`, `MACDCalculator` перенесены из цикла в начало файла | claude |
| 2026-05-27 | B-3 | Вероятности TP теперь не уходят в минус: `max(0.1, 0.7 - i * 0.2)` в `generator.py` | claude |
| 2026-05-27 | B-4 | Исправлен `_TF_RANK` в `telegram_bot.py`: логичный порядок 5m→15m→1h→4h→1M→3d→1w→1d | claude |
| 2026-05-27 | B-5 | TP уровни сортируются по близости к цене перед присвоением вероятностей в `generator.py` | claude |
| 2026-05-27 | B-6 | Упрощена логика `rsi_a_ok` в `entry_spot_filters.py`: `rsi_a is None or rsi_a < 70.0` | claude |
| 2026-05-27 | P-1 | Все компоненты (`ExchangeManager`, `Database`, `SignalGenerator` и др.) инициализируются один раз до `while True` | claude |
| 2026-05-27 | P-3 | `subprocess.run` заменён прямым вызовом `broadcast_signals` через `_notify_telegram()` | claude |
| 2026-05-27 | A-1 | `main()` декомпозирована: `_build_components`, `_analyze_timeframe`, `_run_iteration`, `_export_results`, `_notify_telegram` | claude |
| 2026-05-27 | A-3 | Двойные fallback-импорты в `generator.py` убраны — чистые относительные импорты | claude |
| 2026-05-27 | Q-1 | 30+ `print()` в `run_real.py` заменены на `logger.info/warning/error` | claude |
| 2026-05-27 | Q-2 | Добавлены тесты: `tests/test_indicators.py`, `tests/test_signal_generator.py`, `tests/test_filters.py` — 41 тест | claude |
| 2026-05-27 | A-2 | `src/main.py` удалён, `run.py` перенаправлен на `run_real.main()` | claude |
| 2026-05-27 | A-4 | Дефолтный путь сигналов обновлён на `data/signals.json` в `run_real.py` и `telegram_bot.py` | claude |
| 2026-05-27 | L-1 | `_check_volume` улучшен: 3 условия (объём выше нормы, устойчивость, нет поглощения) | claude |
| 2026-05-27 | L-2 | `track_signals.py` — рефакторинг: `print()` → `logger`, упрощена структура | claude |
| 2026-05-27 | Q-4 | Добавлен `AnalysisConfig` dataclass в `config.py`; убраны цепочки `.get()` в `_build_components` | claude |
| 2026-05-27 | L-5 | Market Sentiment: `market_sentiment.py` + `fetch_long_short_ratio`/`fetch_open_interest` в BinanceParser/ExchangeManager; корректировка confidence ±0.10; блок в Telegram с L/S, OI, FR | claude |

---

## Как работать с этим файлом

1. Нашёл проблему → добавь строку с `[ ]`
2. Взял в работу → смени на `[~]`
3. Исправил → смени на `[x]` и добавь запись в **Журнал изменений**
4. Критичная проблема → ставь `[!]`
